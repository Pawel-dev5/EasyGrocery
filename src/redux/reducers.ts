import global from 'redux/slices/global';
import socket from 'redux/slices/socket';
import lists from 'redux/slices/lists';
import notifications from 'redux/slices/notifications';

export default {
	global,
	socket,
	lists,
	notifications,
};
